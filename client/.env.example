# Base URL of the backend API.
#  - Dev: leave EMPTY — Vite proxies /api to localhost:5000.
#  - Production (Vercel): set to your Render API URL, no trailing slash and no /api,
#    e.g. VITE_API_URL=https://eduboard-api.onrender.com
VITE_API_URL=
